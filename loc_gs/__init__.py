"""Loc-GS: localization-oriented Gaussian feature fields.

The public pipeline reconstructs SuperPoint descriptor and detector maps from a
Gaussian feature field, then uses geometric matching and PnP for camera
relocalization.
"""

__version__ = "0.1.0"
